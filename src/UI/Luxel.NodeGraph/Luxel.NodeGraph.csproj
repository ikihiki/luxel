<Project Sdk="Microsoft.NET.Sdk">

  <!-- 汎用ノードエディタの canvas 非依存コア (ADR-0009 / ToDo 25)。
       テキスト新スタック (Luxel.Document / ADR-0006) の骨格を鏡写しにした
       不変状態 + Transaction + 履歴のデータ層。依存ゼロ (2D ベクトルは BCL の
       System.Numerics.Vector2)。純射影ジオメトリ (GraphGeometry) は S3 で足す。 -->
  <ItemGroup>
    <InternalsVisibleTo Include="Luxel.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Shared\Luxel.Mathematics\Luxel.Mathematics.csproj" />
    <Using Include="Luxel.Mathematics" />
  </ItemGroup>

  <PropertyGroup>

    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
