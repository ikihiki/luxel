# Luxel Playground Browser smoke tests

The Gallery site exporter and its parent Playground shell no longer exist. This package keeps the standalone Slang browser-runtime smoke contracts used by CI.

```bash
cd tests/Luxel.Playground.Browser.E2e
npm ci
npm test
```
