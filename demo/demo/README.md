# Browser acceptance demo

This deterministic project exercises the production Browser Editor host.

- `Player` owns child entity `Coin`.
- `Player.cs` is the text save/reload fixture.
- `Coin.material.json` is the material/node-graph fixture.
- `coin.svg` exists; the optional normal map is intentionally missing to produce one warning.
