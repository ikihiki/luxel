# NEXT — 「次へ」で進む実装キュー

ユーザーが**「次へ」**とだけ言ったら、AI はこのファイルの手順に従って次のタスクを 1 つ進める。

## 「次へ」と言われたときの手順

1. このファイルの**実行キュー**から、未チェック (`[ ]`) の最初のエントリを選ぶ。`(着手中: ...)` メモがあればそれを再開する。
2. エントリが指すタスク MD を**全文読む** (背景・実装方針・罠・検証がそこに揃っている)。[README.md](README.md) の共通規約 (ビルド/テスト/golden 運用/UiComponent 規約/決定性) も従うこと。
3. 着手時: エントリ末尾に `(着手中: YYYY-MM-DD)` を書き足してから作業を始める (中断されても次セッションが再開できる)。
4. 実装 → 下の**完了の定義**を全部満たす → キューのチェックを `[x]` にし、`(着手中)` メモを消す。
5. **タスクの全ステージが終わったら**: タスク MD を削除し、README.md の一覧表から行を削除し、仕様は Gallery の Docs ストーリーへ現在形で書く (既存運用)。
6. 作業中に見つかった穴・新タスクは ToDo/ に新しい MD として追加し、このキューの適切な位置に 1 行足す。
7. 1 回の「次へ」で進めるのは**キュー 1 エントリまで**。早く終わっても次のエントリへ勝手に進まない (ユーザーがまた「次へ」と言う)。

## 完了の定義 (全エントリ共通)

- [ ] `dotnet build` / `dotnet test` が通る (新規ロジックには GPU 不要の単体テスト)
- [ ] e2e: `dotnet run --project src/Luxel.Gallery -- vk e2e` が通る。golden 差分は意図分のみ (`--update` 後に `git diff --name-only -- goldens` で意図外を戻す — README 参照)
- [ ] タスク MD 記載のデモストーリー/Docs 追記を実施 (該当があれば)
- [ ] `dotnet format` 相当のスタイルで綺麗 (リポジトリは dotnet/docs の .editorconfig)
- [ ] コミット済み (conventional commits 風: `feat(particles): ...` 等、日本語本文可)

**ユーザーに聞くのは**: タスク MD に「ユーザーに確認」と明記がある箇所、破壊的な選択、スコープの増減だけ。それ以外は MD の記述を正として自走する。

## 実行キュー (上から順)

### M1 — 基盤 (golden をきれいにしてから土台)

- [x] **Q01**: [13 日本語フォント同梱](13-e2e-japanese-font.md) — 全 golden 再生成を伴うため最初。完了 = e2e で日本語が出る + golden がフォント同梱由来で安定
- [x] **Q02**: 12 メンテ: Docs stale + dx golden — golden を新フォント基準で整えた直後に片付ける
- [x] **Q03**: 14 FixedUpdate + 描画補間 — ゲーム/物理の時間基盤。既存デモ (Knockdown) を移行済み。完了 = FixedUpdate フェーズ/蓄積器/Alpha + InterpolatedTransform + 単体テスト + Demos/Framework/DrawInterpolation + Docs/Framework 追記
- [x] **Q04**: [19 capstone ①](19-standalone-game-shipping.md) **ステージ A: 骨組み + publish 早回し** — タイトル画面だけの LuxelCavern を作り publish チェックリスト 1〜4, 7, 8 を 1 周 (直すのは Luxel 本体側)。**MD はまだ削除しない** (2026-07-06 完了: samples/LuxelCavern 骨組み + shaders publish 修正。詳細は 19 MD 進捗節)
- [x] **Q05**: [21 DevTools ゲーム規模対応](21-devtools-game-scale.md) **ステージ ①: A (ECS スケール) + C (DevStats) + D (FixedUpdate/timescale) + E (WithDevTools) + F (fps 化) + DebugDraw コア** — 完了 (2026-07-07)。A/C/D + DebugDraw コア (2026-07-06) に加え、**E (WithDevTools 集約: 新 `Luxel.Framework.DevTools` + `IFramePublisher`、Cavern を移行) / F1 (FrameChannel リング, 書き手ゼロ割り当て + 二読者 seqlock) / F3 (WebSocket push, latest-wins, 実機 chrome 検証) / F2 (内蔵版プール化) / F4 (MJPEG 不採用) / Docs (Docs/DevTools に「ゲームを観測する」節)** を実装。単体テスト 805 passed、e2e 65/65 diff 0。**MD はまだ削除しない** — B の物理 gizmo (ステージ③ = Q14) 完了まで残す。詳細は 21 MD 進捗節。

### M2 — 2D ゲーム機能 (capstone ① の部品)

- [x] **Q06**: 15 セーブ/ロード + 設定 (2026-07-06 完了) — A: `WorldSave.Serialize/Deserialize` (Friflo EntitySerializer + version ラッパ + `[ComponentKey(null)]` 除外)。B: `SettingsStore.Get<T>()`→`Signal<T>` + `IFileStore` (インメモリ/物理) + 破損 .bak 退避。新プロジェクト `Luxel.Settings`。デモ Demos/Framework/SaveLoad・Settings (golden 各 1)、Docs/Runtime 「永続化」節。単体テスト 14。ゲーム側配線 (保存先 %APPDATA%/ゲーム名、セーブ/設定 UI、InputBindings 永続化) は **Q13 (ゲーム組み上げ)** で実施。
- [x] **Q07**: 17 カメラコントローラ (2026-07-06 完了) — `CameraRig2D` (Luxel.TwoD): 追従 (デッドゾーン + フレームレート非依存の指数平滑) / ワールド境界クランプ / 画面シェイク (固定シード xorshift) / ズーム平滑。`OrbitCamera` (Luxel core): yaw/pitch/distance → viewProj + Orbit/Dolly。`RectF` 追加。単体テスト 11、デモ Demos/TwoD/CameraRig (golden)、Docs/TwoD・ThreeD にカメラ節。3D の follow/shake は MD 通りスコープ外、シェイクは平行移動のみ (回転は任意)。
- [x] **Q08**: 18 スプライトアトラス + タイルマップ (2026-07-06 完了) — A: プリミティブに `srcX/srcY` 追加 (`GpuPath` 64→72B + raster2d fine/bounds シェーダ) でアトラス任意サブ矩形サンプリング (clamp で隣接に滲まない、既存 golden 全無変更を実証)。B: `SpriteAtlas`/`SpriteRect`/`SpriteAnimation`/`Scene2D.DrawSprite`/`ImageSubRect` + `SpriteAtlasStep`。C: `TileSet`/`TileMap` (CSV + Tiled `.tmj` import) + チャンク描画 `AppendChunk` + 可視チャンク `VisibleChunks` + 保持型 `TileMapLayer` (可視チャンクのみ UiNode 実体化・dirty 再構築) + 衝突 `QueryAabb`/`Sweep` (物理非依存 AABB グリッド、軸分離)。単体テスト 32、デモ Demos/TwoD/Sprites・Tilemap (vk golden 各1)、Docs/Gpu にスプライトアトラス/タイルマップ節。**MD 削除済み**。
- [x] **Q09**: 16 パーティクル (2026-07-06 完了) — 新プロジェクト 4 本 (Luxel.Particles コア + .TwoD/.ThreeD/.UI)。コア: ParticleSystem/Buffer/CpuSimulator/Value(Const/Range/Curve)/Color/Config + Xorshift64 + per-particle tint + Forces フック。.TwoD: ParticleNode (RetainedCanvas ContentColors)。.ThreeD: ParticleBillboards + billboard.slang (カメラ向き instance quad) + Spherical 放出。.UI: ParticleView [UiComponent]。JSON: ParticleConfigJson 往復 + ParticleConfigStep。BreakoutStory を新 ParticleSystem に dogfood 置換。単体テスト 24、デモ Demos/TwoD/Particles・ParticleView・Demos/3D/Particles (vk golden 各1) + Docs/Gpu パーティクル節。**MD 削除済み**。
- [x] **Q10**: 10 Audio ストリーミング (2026-07-06 完了) — `IAudioStream`(int Read(Span<float>)) + `WavStream`(依存なし RIFF パーサ、16bit/float32、逐次) + `LoopingStream`(継ぎ目なしループ) + `StreamingVoice`(StreamMixerSink 踏襲の毎 Tick Pump + キュー深さ<3 補充、float→16bit 量子化、リングバッファ)。単体テスト 10 (WAV デコード/チャンク境界/終端/float32/ループ/ポンプのキュー維持・終端停止/headless 例外なし)。Docs/Runtime にストリーミング節。ogg (NVorbis) は次段・mp3 はスコープ外。**実機スモークストーリーは未追加** (RealWindowOnly = golden 非対象、実窓+実デバイス必須でこの環境で検証不可。backend の submit/BuffersQueued 経路は StreamMixerSink と共通で実証済み)。**MD 削除済み**。
- [x] **Q11**: 01 ScriptSystem + csx hot reload (2026-07-06 完了) — 新プロジェクト `Luxel.Scripting.Framework`: `ScriptSystem` (安定ラッパ Attach + Reload、コンパイル失敗/実行時例外で旧維持+診断公開) + `ScriptSystems`/`ScriptGameGlobals` + `IScriptSource`(Memory/File)。デモ `Demos/Scripting/HotReload` (CodeEditor で .csx 編集 → Apply で箱の動きが変わる、構文エラーで旧ロジック継続 + 診断、Canvas2D で決定的、golden 3枚)。ゲームスクリプト用 ScriptHost はストーリーローカル Lazy (typeof(BoxGlobals))。単体テスト 6、e2e 63/63。Docs/Scripting 面③を実装済みに。**MD 削除済み**。
- [x] **Q12**: 21 DevTools ステージ②: B の 2D gizmo (2026-07-06 完了) — DebugDraw コアの上に `Gizmos2D` (TileCollision=衝突タイルワイヤ / Sweep / CameraRig=デッドゾーン+境界、Luxel.TwoD) と `ParticleGizmos.Emitter` (エミッタ十字+alive 数、Luxel.Particles.TwoD) を追加。OFF 時ゼロ割り当て。`ParticleSystem.EmitPosition` 公開。単体テスト 4 (DebugDrawTests に追記=静的状態直列化)、デモ Demos/TwoD/Gizmos2D (Canvas2D=Skia可・決定的、vk golden)。物理 gizmo はステージ③ (Q14)。**MD はまだ削除しない**

### M3 — capstone ① 完成

- [x] **Q13 (完成)**: **capstone ①「Luxel Cavern」** (2026-07-07 完成、S1〜S17)。`samples/LuxelCavern` (Core 純ロジック + 実時間 exe)。合流済: Q05-E/F (DevTools オーバーレイ + fps + DebugServer 配信) / Q06 (セーブ・設定・キーバインドの %APPDATA% 配線)。**19 MD は削除済み** (仕様は `samples/LuxelCavern/README.md` + git 履歴)。(記録: **大タスク・複数セッション**。**S1 済**: CavernSim (プレイヤー物理) + CavernLevel + Game/Cavern golden。**S2 済**: 収集/扉/トゲ+HP/巡回敵/無敵+ノックバック+シェイク。**S3 済**: 飛行敵 + 演出イベント + 松明炎/コイン/撃破パーティクル (tint)。**S4 済**: チェックポイント + CavernSave (進捗 JSON 往復)。**S5 済**: GameFlow 状態機械 + CavernHud (HP/コイン/鍵、world-anchored)。**S6 済**: .csx 敵 AI ドッグフーディング。**S7 済**: 実時間 exe プレイアブル化 (LuxelHostBuilder+GameScene+GameLoop, WindowSystem 提示, KeyboardSource 操作)。**S8 済**: publish 本番 + 配布検証 (self-contained フォルダ 120MB, リポジトリ外 vk/dx exit 0, single-file は保留)。**S9 済**: タイトルフロー + %APPDATA% オートセーブ (checklist 6, `CavernPersistence`)。**S10 済**: **オーディオ (BGM + イベント SE)** — Q10/Luxel.Audio ドッグフード。`CavernSfxDetector` (sim の出来事→SE、純ロジック)、`CavernSfxBank` (CPU 合成・外部アセット不要)、`CavernAudio` (BGM ループ + AudioMixer ワンショット + Master/Music/Sfx バス)、exe は `UseAudio()` で XAudio2。`CavernSim.JumpedThisStep` 追加。テスト `CavernSfxDetectorTests` 9 + `CavernAudioTests` 3、全 775 passed、e2e 65/65 diff 0。**capstone コア (プレイアブル + 配布 + フロー + セーブ + 音) 達成**。**S11 済**: **設定画面 (音量) + SettingsStore 永続化 (Q06 B)** — `CavernSettings` (SettingsStore 上、Master/Music/Sfx を Signal で、AutoSave で %APPDATA% へ)、`CavernAudio.BindSettings`+Tick で AudioBus.Volume に反映、`GameState.Settings` + タイトル「S : せってい」→ ↑↓ 選択・←→ 調整・Esc 戻る。`CavernSettingsTests` 3、全 778 passed、e2e 65/65 diff 0。**S12 済**: **Tiled (.tmj) レベル外部化** — Q07 `FromTiledJson` ドッグフード。`levels/cavern1.tmj` を Core.dll に埋め込み (旧 Build 規則から生成 = bit 同一)、`CavernTiled` がタイル層 (FromTiledJson) + オブジェクト層 (coin/key/door/walker/flyer/checkpoint/torch) を CavernSim へ。`CavernTiledTests` 5、全 783 passed、**e2e 65/65 diff 0**。**S13 済**: **レベル読み込みを ResourceSystem 経由に** (ユーザー指示) — `EmbeddedResourceSource` (`res://` スキーム) + `CavernLevelLoader` (インスタンス・static 無し、`res://levels/cavern1.tmj` を byte[] ノードでキャッシュ)、`GameFlow` がローダを受け取り `CavernLevel` の static CreateSim/Torches/Build を廃止。全 783 passed、e2e 65/65 diff 0。**S14 済**: **ゲーム内 DevTools オーバーレイ (gizmo + DevStats)** (Q05-E/Q12 合流) — `CavernDevOverlay` (F1 トグル、`Gizmos2D`+`ParticleGizmos` を `DebugDraw` に溜め Flush、統計パネル、`DevStats.Set` 配信)。exe に VK_F1 + fps 計測。`CavernDevOverlayTests` 4、全 787 passed、e2e 65/65 diff 0。**S15 済**: **single-file publish 対応** (ユーザー指示) — 本文フォント .ttf を exe Content から Core.dll 埋め込みへ移動、`CavernAssets.LoadBodyFont(ResourceSystem)` が `res://fonts/…` を `EmbeddedResourceSource` 経由でロード。`-p:PublishSingleFile=true` で ~86MB 単一 exe、`C:\` から vk/dx とも exit 0 (旧: フォント未検出で失敗)。`CavernAssetsTests` 2、全 789 passed、e2e 65/65 diff 0。**S16 済**: **キーバインド再割当 UI** (ユーザー指示) — `CavernSettings` に `Signal<KeyCode>` バインド (A/D/Space 既定、AutoSave)、`CavernBindings.Apply/Rebind` (プライマリ + 矢印セカンダリを InputAction へ)、`IKeyCapture` を `KeyboardSource` が実装 (生キー取得)、設定画面 6 行化 (音量 3 + キーバインド 3、Enter で割当・Esc キャンセル)。`CavernBindingsTests` 4、全 793 passed、e2e 65/65 diff 0。**S17 済**: **DebugServer 起動 (ブラウザ DevTools 配信)** (ユーザー指示) — `CavernDevServer` (exe, `--devtools [port]`) が `DevToolsListener`+`DebugServer` を起動、`DevStats` (fps/状態/HP) を `/custom` へ、ゲームフレームバッファを `DiagFrame` として `/frame` へ配信。**Claude in Chrome で実機検証**: DevTools UI 表示・GAME(DEVSTATS) ライブ更新・`/frame?format=png` が実画面 960×540 PNG。全 793 passed、デフォルトスモーク exit 0。**capstone 完成 (コア + checklist + 設定 UI [音量/キーバインド] + DevTools オーバーレイ + DebugServer 配信、全達成)**。**残 (任意・未着手)**: DevTools オーバーレイ/設定画面の golden ストーリー化 (視覚回帰)。)

### M4 — 3D 物理・アニメーション (capstone ② の部品)

- [x] **Q14**: 03 CCD + [21](21-devtools-game-scale.md) **ステージ ③ 物理 gizmo 骨格** (2026-07-07 完了)。**CCD**: `RigidBody.Dynamic(ccd: true)` / `PhysicsWorld.AddDynamic(continuous:, maxSpeculativeMargin:)` → Bepu `ContinuousDetection.Continuous`。テスト = 薄い壁へ 150m/s の球 (margin 0.1 で投機接触を無効化) で「CCD なし=貫通 / あり=手前で停止」を両 assert。Docs/Physics に CCD 節、ロードマップから CCD 削除。**03 MD 削除済み**。**物理 gizmo 骨格**: 新プロジェクト `Luxel.Physics.Gizmos` の `PhysicsGizmos.DrawColliders(world, dyn, static, ccd 色)` = ECS の Collider を OBB ワイヤ (箱=実寸/球・カプセル=外接) で描画・動的/静的/CCD を色分け・OFF 時ゼロ割り当て。デモ `Demos/3D/PhysicsGizmos` (等角投影・authored pose で決定的、golden 1)。単体テスト 3 (CCD 1 + gizmo 2)、全 808 passed、e2e 66/66 diff 0。トリガー/接触の gizmo は Q15 で完成 (→ 21 全ステージ済み、**21 MD 削除済み**)。
- [x] **Q15**: 04 接触イベント + トリガー + [21](21-devtools-game-scale.md) **ステージ ③ 完了** (2026-07-07)。**接触イベント**: `LuxelNarrowPhaseCallbacks` が `ConfigureContactManifold` で実接触 (depth≥0) ペアを共有コレクタ `PhysicsContacts` へ収集 → `PhysicsWorld` が Timestep 前後で BeginStep/前ステップ差分 → `ContactEvents` (raw, collidable) を公開。`PhysicsStepSystem` が handle→Entity 逆引きで `ContactEvent {A,B,Phase}` (Entity ベース) へ変換。**トリガー**: `Trigger` component (形状は同居 Collider、静的) を `AddTrigger` で登録、callbacks がトリガー絡みペアは manifold 無効化 (力なし) しつつ検知。**gizmo (21 ③ 完成)**: `PhysicsGizmos.DrawColliders` に trigger 色追加 + `ContactMarkers` (接触中ペア = `PhysicsStepSystem.CurrentContacts` を opt-in 収集、中心間midpoint に十字インジケータ)。デモ `Demos/3D/PhysicsTrigger` (落下球がゴールゾーン通過→enter/exit カウント + 着地接触マーカ、物理を固定 dt で回して決定的、golden 1)。Docs/Physics に「接触イベント + トリガー」節 (ロードマップから項目 2 削除)、Docs/DevTools gizmo 節に追記。単体テスト +4 (接触 begin/end・トリガー通過で速度不変・gizmo trigger 色・contact 十字)、全 812 passed、e2e 67/67 diff 0。**04 MD + 21 MD 削除済み**。
- [x] **Q16**: 05 メッシュ/凸包コライダー (2026-07-07)。**静的メッシュ**: `PhysicsWorld.AddStaticMesh` (三角形スープ → Bepu `Mesh`、Shapes 経由で Dispose 連動) + ECS `MeshCollider.Static(verts, indices, scale)`。**動的凸包**: `PhysicsWorld.AddDynamicHull` (頂点群 → `ConvexHull`、重心 recenter の center を out で返し pose を補正) + ECS `HullCollider.Dynamic(points, ...)` (attach で center 保持、書き戻しで元原点へ)。contact 逆引き map に mesh/hull も追加。デモ `Demos/3D/PhysicsMesh` (波打つ地形メッシュ + 球 + 四面体凸包が載って静定、等角投影で決定的、golden 1)。Docs/Physics に「メッシュ/凸包コライダー」節 (winding=片面/重心オフセットの罠、凸分解・動的メッシュはスコープ外) + ロードマップから項目 3 削除。単体テスト +3 (静的メッシュに球静定・四面体凸包が床貫通なし・ECS mesh+hull attach/静定)、全 815 passed、e2e 68/68 diff 0。**05 MD 削除済み**。
- [x] **Q17**: 09 glTF skin/morph (2026-07-07 完了、skin + morph の 2 スライス)。**skin** (scaffold 結線): `scene_pbr_skinned.slang` を SceneInstanceData 形式 (world+baseColor) + joint バッファに整備 (matIdx/material 版から差替、glTF 規約で skinned node 変換は無視=instance world 恒等、SkinningSystem の既存 InverseBind×jointWorld を流用)、デモ `Demos/3D/GltfSkinned` (RiggedSimple、GPU 頂点スキニング、曲がる棒)。罠: RenderGraph の Write 無しパスはデッドパスカリング。**morph** (greenfield): loader が `primitive.targets`/`mesh.weights`/`node.weights` をパース、`SceneBuilder` が morph デルタ (位置+法線 24B × target × vertex) を upload + `MorphWeights` component 初期化、`SceneAnimationPlayer` が weights channel を適用 (flat float[] を targetCount で slice)、新シェーダ `scene_pbr_morph.slang` (頂点で `pos/nrm += Σ w·δ`)、デモ `Demos/3D/GltfMorph` (手続き的な隆起 morph、weight 0.85)。単体テスト +7 (SkinningSystemTests 3 + GltfLoader morph parse 1 + MorphTests 3)、全 822 passed、e2e 70/70 diff 0・vk/dx 一致。Docs/Motion 更新。**09 MD 削除済み**。**→ M4 (物理・アニメ Q14〜17) 完了、次は M5 = Q18 (3D capstone)**。

### M5 — capstone ② 完成

- [x] **Q18**: capstone ②「Luxel Range」(3D 射的) 完成 (2026-07-07、14 スライス)。`samples/LuxelRange` (Core 純ロジック + 実時間 exe)。**検証達成**: 03 CCD / 04 接触・トリガー / 05 メッシュ地形・凸包小物 / 09 skinned Fox / 16 パーティクル / 10 音 / 15 永続化 / 17 OrbitCamera。exe = playable + 全 3D 描画 + 音 + HUD + **publish 検証 (glTF/全シェーダ同梱・外部 vk/dx 起動 exit 0)**。全 838 passed、e2e 71/71 diff 0。**20 MD 削除済み**。**→ M5 完了 (capstone 2 本達成)、残りは M6 (Q19〜23 エディタ/ツール系)**。(将来の任意磨き = Title/Result のテキスト画面 [フォント同梱要]、フロー logic + HUD は実装済み。) 旧スライス詳細: `samples/LuxelRange/LuxelRange.Core` の `RangeSim` (床 + 薄板ターゲット + CCD 弾 + ContactBegin スコア、残弾制、最初の発射まで物理停止) + `RangeSimTests` 4 + Gallery `Apps/Game/Range` (OrbitCamera 軌道 + クリック発射 + cube_forward、golden vk/dx 一致)。03/04/17 統合済み。**スライス 2 完了**: `RangeTerrain` の起伏メッシュ地形 (05 in-game、同一頂点を物理 AddStaticMesh + scene_pbr_lite 描画に共有 = 絵=当たり) + 外周壁、Gallery で 2 パイプライン描画 (地形 pbr + 的/弾 cube)、winding 検証テスト +1。全 827 passed、e2e 71/71 diff 0・vk/dx 一致。制約: 高速弾の対 Mesh CCD は貫通あり→kill plane で回収。**スライス 3a 完了**: `PhysicsWorld.RemoveBody`/`RemoveStatic` + `RangeSim` の動的 ConvexHull 小物×3 (単位箱ハル=描画一致) + kill plane despawn (KillY 下回りで RemoveBody+DeleteEntity、貫通弾を回収)。テスト +2、全 829 passed。**スライス 3b 完了**: ボーナスゾーン (Trigger + RangeBonusZone マーカ + 黄床マーカ、ContactBegin 小物×ゾーン→+200 一度だけ)、テスト +1、全 830 passed、e2e 71/71 diff 0。知見: 100m/s 弾で動的小物を吹き飛ばすのは対動体 CCD 限界で不確実。**スライス 3c-1 完了**: キネマティック体インフラ (`PhysicsWorld.AddKinematic`/`SetBodyPose`/`KinematicBody` component) + 動く的 Fox (キネマティック箱を X 巡回、命中 +300 + ひるみ 1.5s)。**キネマティックは CCD 弾が確実命中** (動的小物のトンネリング回避)。テスト +2、全 832 passed。**スライス 3c-2 完了**: 動く的を skin モデル (Fox.glb) に差し替え — 別 world で SceneBuilder + 歩行アニメ + SkinningSystem、skin 頂点を instance World=Scale×Rot×Translate(FoxPosition) で配置、scene_pbr_skinned で描画。歩く Fox が golden に、vk/dx skinning 一致。**09 (skin) を capstone ゲーム内で検証達成**。e2e 71/71 diff 0。**スライス 3d 完了**: ゲームイベント層 (`RangeEvent`/`RangeSim.Events`/`ClearEvents`、Shot/TargetHit/FoxHit/BonusScored + 位置) + `RangeSfxDetector` (イベント→SE キュー、Cavern 方式の純ロジック)。テスト +2、全 834 passed。命中パーティクル/exe 音はこのイベントを consume。**スライス 3d-2 完了**: 命中 .ThreeD パーティクルバースト描画 (RangeScene に ParticleSystem+ParticleBillboards、命中イベント位置で放出、RenderGraph パス内で描画)。golden にデモバースト、vk/dx 一致。**16 をゲーム内で検証達成**。**スライス 4 完了**: ゲームフロー (`RangeGame` の Title/Play/Result 状態機械 + 弾切れ後 settle で Result) + ハイスコア永続化 (`RangeSettings` = SettingsStore 上、15 の再利用確認)。テスト +3、全 837 passed、e2e 71/71 diff 0。Title/Result UI + BGM/SE 実音は exe。**スライス 5-1 完了**: exe 骨組み (`samples/LuxelRange/LuxelRange`、Program.cs + RangeRealtimeScene で RangeGame を実時間駆動・attract 動作、地形+cube 描画を Framebuffer 提示、%APPDATA% ハイスコア)。vk/dx スモーク exit 0、**publish で全 3D シェーダ同梱 + リポジトリ外 vk/dx 起動 exit 0** (出荷経路 OK)。罠: DI singleton の二重 Dispose→冪等化。**スライス 5-2 完了**: exe キーボード操作 (KeyboardSource + InputAction、矢印で OrbitCamera 旋回 + Space 発射 + Esc 終了、無操作時は緩い自動旋回)。vk スモーク exit 0。全 837 passed、e2e 71/71 diff 0。**スライス 5-3 完了**: exe に Fox skin 描画移植 + **Fox.glb を assets/ 同梱** → publish に glTF アセット + scene_pbr_skinned 同梱、リポジトリ外起動で bundled Fox を解決・描画 exit 0 (**glTF publish 検証達成**)。exe 描画 = 地形 + 的/小物 + 歩く Fox。全 837 passed、e2e 71/71 diff 0。**capstone ② の検証目標 (03/04/05/09/16 + 10/15/17) + 出荷 (publish/外部起動 vk/dx) は達成**。**スライス 5-4 完了**: exe に命中パーティクル移植 (ParticleBillboards、命中イベント位置で火花バースト)。exe 描画が Gallery と一致 (地形+的/小物+Fox skin+火花)。vk スモーク exit 0、全 837 passed、e2e 71/71 diff 0。**スライス 5-5 完了**: exe BGM/SE 実音 (10) — `RangeSfxBank` (SE/BGM を CPU 合成) + `RangeAudio` (Mixer ワンショット + AudioSource BGM + AudioBus、React でイベント→SE)、exe が UseAudio + PlayBgm + 毎更新 React。テスト +1、全 838 passed、e2e 71/71 diff 0。実音は RealWindowOnly。**残 (最終・任意)**: exe Title/Result UI オーバーレイ (Rasterizer2D、スコア/ハイスコア/残弾/レティクル) → 完了で **20 MD 削除**。capstone ② の全検証目標 + 出荷 + 描画/操作/音は達成済み。詳細は 20 MD 進捗節。

### M6 — エディタ/ツール系 (ゲーム完成後。順不同で良い)

- [x] **Q19**: 11 デバッグツール (2026-07-08 完了、A/B/C の 3 スライス)。**B (入力記録 → 決定的リプレイ)**: `Luxel.UI` に `InputKind`/`RecordedInput`/`InputRecording` (JSON 往復 + version) + `InputScript.ToPlayCode` (記録→ `d.Click`/`d.Drag`/`d.Type`/`d.Key`/`d.Wheel` 生成) + `InputRecorder` (UiHost の新フック `InputCaptured`+`Ticked` 購読、フレーム番号付き) + `InputReplayer` (固定 dt 再生)。UiHost に構造化入力フック (PointerMove/Wheel 含めロスなし・0 コスト)。デモ `Demos/Framework/InputReplay` (golden vk/dx)。**A (Gallery 常設 Console タブ)**: 下ペイン Tabs 4 枚目に継続 REPL、`ReplConsole` を `Stories/ReplConsole.cs` に共通化 (Repl ストーリーと共用)、`_console` フィールドで session/履歴を chrome 再構築・ストーリー切替越しに保持、`ScriptGlobals` 遅延解決 ctor で Log が現ストーリー追従。実機 DevTools `/cmd` で選択→REPL 描画→フォーカス確認 (screenshot)、eval は Repl ストーリー e2e で回帰担保。**C (外部デバッガアタッチ)**: `ScriptHost` に `filePath:` option (実在 .csx 絶対パス→PDB が実ファイルを指す→VS/VS Code がブレーク束ねられる)、単体テストで実パス由来の行番号/スタックトレースを自動担保、Docs/Scripting ⑤⑥ に入力リプレイ + アタッチ手順を記述 (対話ブレーク着弾は人手検証)。単体テスト +7、全 845 passed、vk e2e 72/72 diff 0。**11 MD 削除済み** (仕様は Docs/Scripting + Docs/Framework)。
- [x] **Q20**: 02 Strudel REPL の CodeEditor 化 (2026-07-08 完了)。`StrudelCodeLanguage` (`ICodeLanguage`、Gallery/Stories): `StrudelEval.Evaluate` でパース → `StrudelEvalError` (MiniNotation の位置付きエラーも畳まれる) を `CodeDiagnostic` 波線に写す (評価するだけで音は出さない) + 文脈補完 (クォート内=音色 / `.` 直後=メソッド / それ以外=関数)。StrudelStory のライブブロックを `TextArea` → `CodeEditor` (ガター/等幅/横スクロール、MonoFont + LanguageService)、**Ctrl+Enter** を `OnKeyIntercept` で横取り → 現ブロック Run (音の連続性のスロット所有権はそのまま、通常 Enter は改行)。play を拡充 (2 ブロック実体化 → 未知メソッド型付けで `DiagnosticCount>0` + `diag` golden の赤波線 → Ctrl+Enter で `LastRunOk`)。単体テスト +8 (診断位置/行マップ/補完文脈、E2e.Tests に配置 = Gallery 参照が要るため)、golden Repl/diag/Docs_Strudel を vk/dx 更新、全 845 passed + vk e2e 72/72 diff 0。DocsStrudel 更新 (v1 スコープ外から Ctrl+Enter 削除 + エディタ統合節)。**02 MD 削除済み** (仕様は Docs/Strudel)。
- [x] **Q21**: 06 CodeEditor 補完磨き込み (2026-07-08 完了、3 点セット)。**① 入力絞り込み**: 補完 open 後もタイプで閉じず、トリガー (識別子開始) からの断片で `_compAll` をローカル絞り込み (prefix 優先→大小無視包含)、Backspace で戻る・トリガーより前 or 0 件で閉じる (`CompletionCount` はフィルタ後、IME 合成開始で閉じる)。**② クリック選択**: `_popupBg` 相対の行ヒット (transform 追従・`Active` で可視行ゲート) + マウスホバー行ハイライト。**③ dwell ホバーツールチップ**: `AddAnimation` の**フレームカウント** dwell (wall-clock 非依存=決定的)、`OnEditorMove` で位置追跡→30 フレーム留まると `LanguageService.Hover` をフローティング表示 (`HoverTipOpen`/`HoverTipText` 公開)。単体テスト +4 (絞り込み/Backspace/クリック挿入/dwell、GPU 不要)、story Completion に play 拡充 (filtered/clicked/hover golden 追加・inserted は clicked に改名) vk/dx 更新、Docs/Editor 更新。**副次修正**: E2E で Strudel が実 XAudio2 を初期化して Vortice のエンジンコールバック GC レースで稀にクラッシュしていた (Q20 の Ctrl+Enter play が誘発) → `StrudelStory.HeadlessAudio` を E2e ランナーが立てて NullAudioBackend を使う (golden 不変・音は snap しないので影響なし)。全 849 passed + vk e2e 73/73 diff 0 (2 回連続クリーン)。**06 MD 削除済み** (仕様は Docs/Editor)。
- [x] **Q22**: 08 Strudel 音楽機能拡張 (2026-07-08 完了、A〜E の 5 サブタスク)。**A** = 記法 `!` 繰り返し + `.` グループ (MiniNotation)。**B** = scale/chord (`Pattern.FlatMapValues` + `Controls.Scale`/`Chord` + StrudelEval `chord`/`.scale`)。**C** = filter/delay (ControlMap 5 フィールド + StreamMixerSink に biquad LPF + フィードバックディレイ + StrudelEval `.lpf`/`.delay` 等)。**D** = wav 音色 (`SampleInstrument` = WavStream 全展開→モノ化→Note ピッチ/レートリサンプル、`InstrumentBank` 登録で `s("名前")`、実アセットデモは見送り)。**E** = MIDI out (`MidiOutSink` [`IEventSink`] = ScheduledEvent→note on/off を絶対秒で溜め `Pump(now)` 送出、`IMidiOut`/`NullMidiOut`/`WinMmMidiOut` [winmm LibraryImport]、実送出は実機スモーク)。単体テスト +30 (記法 7 / eval 8 / audio 7 / midi 8)、全 879 passed、vk e2e 73/73 diff 0。**08 MD 削除済み** (仕様は Docs/Strudel)。
### M7 — テキストエディタ新スタック (ADR-0006、[22](22-editor-decoration-redesign.md) の S1〜S8。既存コントロールは触らず新規追加。**22 MD は全ステージ完了まで残す**)

- [x] **Q23**: 22 新スタック S1 (2026-07-08 完了) — 新プロジェクト `Luxel.Editor` (依存ゼロ・canvas 非依存)。`TextDoc` (行索引・不変)、`ChangeSet` (retain/replace セクション列 + `Apply`/`MapPos(assoc)`/`Invert`/`Compose`、CodeMirror6 流)、`ChangeSpec`、`EditorSelection`/`SelectionRange` (native 複数レンジ + ソート/マージ正規化 + Map)、`EditorState`/`Transaction`/`TransactionSpec` (不変スナップショット、複数編集を 1 ChangeSet に)、`History` (undo/redo、1 Transaction=1 undo、Compose で連続タイプ畳み)。単体テスト `EditorCoreTests` 22 本 (行索引/Apply/MapPos 境界/Invert 往復/**Compose fuzz 500 ケース**/選択正規化/マルチカーソル編集 1 undo/coalesce)。全 901 passed。UI 未接続なので golden 影響なし・e2e 対象外。**知見**: 合成 ChangeSet の MapPos は挿入の由来がフラット化で失われ置換境界の右寄せが逐次と一致しないことがある (CM6 も同様) → 実害なし (選択は単発 ChangeSet を通す・畳み undo は Apply を使う)。Compose の不変条件は Apply 一致で担保
- [x] **Q24**: 22 S2 (2026-07-08 完了) — 装飾を第一級状態に。`Decoration` 種別 (Mark[前景色/背景/下線・波線/囲み] / Widget[アンカー・置換、size+key] / Line[行背景] / LinePrefix[行頭番号] / Block[縦バー/背景]) + `AffectsLayout` 分類 (色/widget/prefix=要再レイアウト、背景/下線/囲み=オーバーレイのみ) + 各 `Map(ChangeSet)` (排他/包含端・削除で消えたら drop)。`DecorationSet` (ソート/Map/Touching/OfKind) + `DecorationTable` (owner 別・不変・Map)。`StateEffect`/`SetDecorations`/`RemoveDecorations` + `IDecorationProvider` (同期) + `DecorationProviders.Collect`。`EditorState.Decorations` 統合 (編集で既存を写像・effect は新座標・undo/redo も追従)。非同期プロバイダの古い結果は発行時からの ChangeSet で写してから set (view 配線、テストで実証)。単体テスト `EditorDecorationTests` 18 本、全 919 passed。**副次修正**: `ChangeSet.MapPos` の先頭ガードを `pos<=0` → `pos<0` に (先頭挿入を assoc>0 で挿入の後ろへ写すため。既存 core テスト全緑で回帰なし)。UI 未接続なので golden 影響なし・e2e 対象外
- [x] **Q25**: 22 S3 (2026-07-08 完了) — `EditorGeometry` (純射影、TextLayout 使用、選択状態を持たない)。`Luxel.Editor` に Luxel.Typography 参照追加。`DisplayLine` = 1 ソース行の TextLayout + **ソース桁↔表示桁のランレングス写像** (`Seg`): 行頭 prefix (ソース 0 文字→表示 k)・行内置換 widget (ソース n→表示 1 ボックス U+FFFC)・前景色ランを畳む。`EditorConfig` (Fonts/FontSize/Wrap/MaxWidth/LineHeight/DefaultColor)。API: `CaretRect(srcOff)`/`HitTest(x,y)→srcOff`/`SelectionRects(from,to)` 行跨ぎ/`OverlayRects()` (背景/下線/波線/囲み/行背景/ブロック、レイアウト非依存)/`WidgetSlots()`/`MoveVertical(off,dir,ref goalX)` 折返し・隣行対応/`ContentHeight`/`LineTop`。**行キャッシュは内容+レイアウト依存装飾を鍵に**(オーバーレイ変更では TextLayout 再構築せず=再生囲み 60fps の根拠、前景色変更では再構築 — テストで Assert.Same/NotSame 実証)。単体テスト `EditorGeometryTests` 14 本 (座標往復/prefix/widget 置換+端ヒット/色ラン/オーバーレイ/キャッシュ/選択/縦移動 goal-x/折返し、GPU 不要 VectorFont.LoadSystem)、全 933 passed。アンカー widget と IME 合成の写像は同機構で S5/S4 に足す。UI 未接続なので golden 影響なし・e2e 対象外
- [x] **Q26**: 22 S4 (2026-07-08 完了) — canvas 接続。`EditCommands` (純関数 `(EditorState)→Transaction`: InsertText/Newline/Delete前後/MoveLeft・Right/Home・End/SelectAll/SetSelection、**マルチカーソル対応**=全レンジに 1 ChangeSet、サロゲートペア境界)。`TextEditorView` [UiComponent] (Luxel.Controls、Luxel.Editor 参照): EditorState/History/EditorGeometry を保持し入力を Transaction 化、ジオメトリの矩形を塗る薄いビュー。色別ノード (DrawColorRuns+node.Color)・選択・キャレット(複数レンジ対応)・オーバーレイ(背景/下線/波線/囲み、ContentColors)・スクロール・キャレット点滅・縦移動(goal-x はジオメトリ)。**IME** は composition を一時状態に挿入+下線/対象強調の装飾で描く方式 (確定まで履歴不変)、ITextInput/TSF 実装。単体テスト `EditorCommandTests` 12 本 (全 945 passed)。story `Controls/TextEditorView/Basic` (edit/undo の 2 play)、**vk/dx golden 各 4 枚生成** (日本語+ラテンのプロポーショナル・複数行選択・undo を実証)、**full vk e2e 75 plays diff 0** (既存無回帰)。Reference/Overview golden は不変。オーバーレイ描画は S6 の診断/検索で golden 補強
- [x] **Q27**: 22 S5 (2026-07-08 完了) — インライン widget ホスト + 行頭/ブロック装飾。行頭 prefix (番号) と Block (縦バー/背景) は S3/S4 で既に描画されるため、核心は widget ホスト。**geometry**: アンカー widget (ソース 0 文字→表示 1 スロット) 対応を追加 (置換に加え)。**view**: `WidgetResolver` (不透明キー→Widget)、`WidgetSlots` に `Layout`+`Realize`、`OnChildNeedsRealize` で再実体化 (箱サイズは装飾宣言なので高さ吸収不要)、消えたキーは Dispose。`Providers` (IDecorationProvider リスト) + `RunProviders` (状態変化で装飾を反映、S6 のシンタックス/診断がここに載る)。**罠修正**: RealizeWidget の worldOrigin に rect を足すと `CreateRoot` の `WorldPos=worldOrigin+Offset` で二重計上 → 描画は正しいが WorldPos がずれ `d.Click(widget)` が外れる。container 原点を渡す形に修正。単体 `Widget_Anchor` 追加 (全 946)。story `Controls/TextEditorView/Widgets` (行頭番号 + ◯→チェックボックス置換 + 左縦バー、クリックでトグル)、**vk/dx golden 各 2 枚**、**full vk e2e 76 plays diff 0**。行内 widget が状態を持ちクリックで再実体化されることを golden で実証。**追修正**: ブロックの縦バーと行頭番号が左端で重なっていた → `BlockDecoration.Indent` を追加しブロック内の行を右へずらす (バーは確保した余白に描画)。geometry の caret/hit/選択/widget/text 描画に `LineIndent` を反映、`BlockIndent` テスト追加 (全 947)、Widgets golden 再生成
- [x] **Q28**: 22 S6a (2026-07-08 完了) — コードエディタ核心。**view**: TextEditorView に行番号ガター (`ShowLineNumbers`、固定 x・縦スクロール追従・クリップ、本文は `ContentX`=ガター幅ぶん右へ)。**プロバイダ** (Luxel.Controls、`IDecorationProvider` アダプタ): `SyntaxHighlightProvider` (`ISyntaxHighlighter`→Mark.Foreground、テキスト+テーマでキャッシュ=要件4)、`DiagnosticsProvider` (`ICodeLanguage.Diagnose`→Mark.Underline 波線、Line/Col→オフセット変換=要件3)、`CurrentLineProvider` (主キャレット行→LineDecoration)。`TokenColor` は既存 CodeEditor の写像を流用。**Providers に足すだけ**で色分け+波線+現在行が付く (view 新規コードはガターのみ)。単体 `EditorProviderTests` 4 本 (全 951)、story `Controls/TextEditorView/Code` (C# 色分け + "int broken = ;" に赤波線 + 行番号 + 現在行、golden 目視確認)、vk/dx golden 各 2 枚、full vk e2e 77 plays diff 0。補完/ホバー/検索/行操作は Q28b
- [x] **Q28b**: 22 S6b (2026-07-08 完了) — コード編集操作。**行操作** (EditCommands、canvas 不要): `MoveLineUp/Down` (Alt+↑↓)、`DuplicateLine` (Shift+Alt+↓、Ctrl+D は S7 マルチカーソル用に温存)、`ToggleLineComment` (Ctrl+/、インデント検出・全行コメント済みなら外す)。**検索**: `TextSearch.FindAll` (純関数マッチ収集) + view の `SetSearch`/`FindNext`/`FindPrev`/`ReplaceAll`/`SearchMatchCount`/`SearchCurrent`、マッチを `Mark.Background` の "search" 装飾で描画 (現在マッチは強め)、選択+スクロール追従。view にキーバインド追加。単体 `EditorCommandTests` +8 (行操作 6 + 検索 2、全 959)。story `Controls/TextEditorView/Edit` (行移動/複製/コメント + 検索 4 マッチ + 全置換)、vk/dx golden 各 3 枚、full vk e2e 79 plays diff 0。補完/ホバーは Q28c
- [x] **Q28c**: 23 P1〜P3 (2026-07-08 完了) — 浮遊 UI placement エンジン。**P1**: `PopupPlacer.Solve` (純関数、`AnchoredPlacement { Side/Align/Flip/Shift/Gap/Margin/MaxW/MaxH }` → 希望側→入らねばフリップ→交差軸シフト→サイズクランプ)。単体 `PopupPlacerTests` 9 本 (フリップ/シフト/クランプ/揃え/Margin/NoFlip)。**P2**: `OverlayEntry.Anchored` を追加、`UiHost.Place` を `PlaceRegion` (Center/Edge/Corner) + ソルバ (anchored) に分離。**既存 Below/Above は `AnchoredOf` で同等 Anchored に写して同じソルバを通す** (Margin=0 で旧挙動一致) → Select/Dropdown/Tooltip/ColorPicker が無改修で端フリップ/シフト/クランプ獲得、**full vk e2e 79 plays diff 0** (挙動保存を実証)。アニメは解いた方向 (actualSide) でスライド。**P3**: `ContextMenu` が `ctx.Host.Width/Height` を viewport にソルバで配置 → 端でクランプ/フリップ。全 968 passed。フリップの視覚 golden は端レイアウト強制が煩雑なため単体テストで担保 (統合は diff 0 で実証)。**S6c 補完はこの Popper 上に (Q28d)**
- [x] **Q28d**: 22 S6c (2026-07-08 完了) — 補完ポップアップ + dwell ホバー。`TextEditorView.LanguageService` (`ICodeLanguage`)。**補完**: Ctrl+Space で開き、`_root` 直下・高 Z (エディタのクリップ外) に描画、**位置は `PopupPlacer.Solve` で CaretRect にアンカー** (画面端でフリップ)。タイプで絞り込み (prefix→包含、トリガー=識別子開始)、↑↓ ナビ・Enter/Tab 確定・Esc 閉じ・行クリック確定。**dwell ホバー**: `onMovePos` で位置追跡 + フレームカウント (決定的) の AddAnimation、留まると `LanguageService.Hover` をポインタ近傍に Popup 表示。`CompletionOpen`/`CompletionCount`/`HoverTipOpen` 公開。story `Controls/TextEditorView/Completion` (complete: 開く→絞り込み→Enter 確定 / hover: dwell)、**vk/dx golden 各 5 枚** (補完リスト + 型ホバーを目視確認)、full vk e2e 81 plays diff 0、全 968 passed。**→ S6 完了 (S6a/b/c)。次は S7 マルチカーソル**
- [x] **Q29**: 22 S7 (2026-07-08 完了) — マルチカーソル (旧 07 統合)。**native 複数レンジなので engine 手術ゼロ・コマンド追加のみ**。`EditCommands.SelectNextOccurrence` (Ctrl+D: 主が空なら語選択→以後同一語の次の出現を追加、ラップ)、`ClearSecondaryCursors` (Esc)、`WordAt`。view: Ctrl+D / Ctrl+Alt+↑↓ (`AddColumnCursor`=主キャレット x に同一 x HitTest で縦列追加、等幅非依存) / Esc 解除 / **IME は主のみ** (合成開始でセカンダリ解除) / セカンダリキャレットは薄色 (ContentColors)。編集は既存の複数レンジ InsertText が 1 ChangeSet で一括 = **1 打鍵で全箇所置換・1 undo** (S1 の native モデルの果実)。単体 `EditorCommandTests` +5 (全 973)、story `Controls/TextEditorView/MultiCursor` (ctrl-d: 3 箇所選択→置換 / column: 縦列→一括挿入)、vk/dx golden 各 4 枚、full vk e2e 83 plays diff 0。**Alt+Click/Alt+Shift+ドラッグは PointerEvent に修飾キーが無く延期** (フレームワーク拡張が要る、キーボード縦列で要件は充足)。次は S8 Strudel
- [x] **Q30**: 22 S8a (2026-07-08 完了) — Strudel ソーススパン配管 + 再生囲み + 行内スライダ。**Luxel.Strudel**: `SourceSpan(Start,Length)` + `Hap<T>.Span` (WithValue/WithTime で保つ)、`Pat.Pure(v, span)` で刻む、`FlatMapValues`/`OpLeftQuery` の生 Hap 構築 2 箇所で伝播、`MiniNotation.Parse(src, baseOffset)` が `ReadWord` の開始位置からアトムに行絶対スパンを刻む。`PatternSpans.ActiveAt(now)` = 点クエリで鳴っているトークンのスパン集合。**view**: `SetDecorations(owner,set)` (push 型・毎フレーム可)。story `Controls/TextEditorView/Strudel` — MiniNotation を点クエリし active トークンを `Mark.Box` で囲む (beat 変化で bd→sd へ移動、**レイアウト非依存=行キャッシュ非再構築**) + 数値 "0.8" を行内スライダに置換。単体 `StrudelSpanTests` 5 本 (全 978)、vk/dx golden 各 2 枚、**full vk e2e 84 plays diff 0** (既存 Strudel パターン無影響=スパンは純メタデータ)。要件「行内 UI + 再生シーケンス囲み」を新スタックで実証。実 REPL 移行 + Docs + MD 削除は Q30b
- [x] **Q30b**: 22 S8b (2026-07-08 完了) — Strudel REPL 移行 + 実配線 + Docs。`StrudelEval.MiniArg` が `s.Pos+1` を `MiniNotation.Parse` に渡しスパンを行絶対に。`StrudelScheduler.ActiveSpans(slot)` (現サイクルの点クエリ)。`TextEditorView.OnKeyIntercept` 追加。**StrudelBlock を CodeEditor → TextEditorView へ移行** (LanguageService 補完 + DiagnosticsProvider 波線 + Ctrl+Enter + 毎フレーム ActiveSpans→Mark.Box、変化時のみ Refresh)。Repl play を `blocks[1].DiagnosticCount` に更新。単体 `StrudelSpanTests` +1 (s().gain() 経由でスパン到達=全 979)、Repl vk/dx golden 再生成 (新エディタ描画)、Docs/Strudel + Docs/Editor を新スタックで記述、**full vk e2e 84 plays diff 0 (2 連続)**。再生囲みは実配線済みだが Session が process-wide static でサイクル位置が走行順依存のため Repl では snap せず (決定的な囲みは Controls/TextEditorView/Strudel が担保)。**→ ToDo 22 (エディタ新スタック S1〜S8) 完成・MD 削除**

### M8 — 排他モード IME (必要になったら。ADR-0008 は Proposed)

- [ ] **Q31**: [24 カスタム IME 候補ウインドウ](24-custom-ime-candidates.md) — TSF `ITfUIElementSink` で OS 候補を抑制 + `ITfCandidateListUIElement` 読み取り + Popup 描画 (排他フルスクリーン対応)。**排他モードが必要になった時点で着手**、実機手動検証 (golden 不可)。ADR-0008

### M9 — 汎用ノードエディタ (ADR-0009、[25](25-node-editor.md) の S1〜S7。既存 Diagram / Animation.Graph は触らず新規追加。**25 MD は全ステージ完了まで残す**)

- [x] **Q32**: 25 ノードエディタ **S1** (2026-07-09 完了) — コアデータ。新プロジェクト `Luxel.NodeGraph` (依存ゼロ・canvas 非依存、2D ベクトルは BCL `System.Numerics.Vector2`)。`GraphElements` (`PortId`/`PortDir`/`NodePort`/`GraphNode`/`GraphEdge`、不変 record)、`NodeGraphDoc` (id→索引辞書で O(1) 引き・辺端点/向き検証・`RemoveNode` で接続辺を掃除・`EdgesOf`)、`GraphChange` (抽象 record + `AddNode`/`RemoveNode`/`MoveNode`/`SetNodeData`/`SetNodeCollapsed`/`Connect`/`Disconnect`、各 `Apply`/`InvertAgainst`。**RemoveNode の逆 = ノード復活 + 接続辺の再接続**) + `GraphChangeSet` (`Apply`/`InvertAgainst` は中間 Doc に対して反転し逆順連結・`Concat`)、`GraphSelection` (ノード/辺 id 集合 + main、`Retain` で削除参照を落とす = 安定 id なので写像不要)、`GraphViewport` (pan/zoom、undo 非対象)、`NodeGraphState`/`GraphTransaction`/`GraphTransactionSpec` (不変スナップショット、遅延 State キャッシュ)、`GraphHistory` (1 tx=1 undo、coalesce で連続移動畳み、viewport 保持)。テキスト新スタック ([[luxel-editor-redesign]]) の骨格を鏡写し。単体 `NodeGraphCoreTests` 19 本 (id 索引/重複・向き・端点検証/辺掃除/各変更 Apply-Invert 往復/ChangeSet 反転で開始復帰/選択 Retain/複数変更 1 undo/coalesce/redo 破棄)。全 998 passed、**vk e2e 84/84 diff 0** (S1 は UI 非接続 = golden 影響なし、ADR-0009 追記も golden 中立を実証)。**MD 仕様との差**: 設計の `Vec2` は `System.Numerics.Vector2`、`History`→`GraphHistory` (名前空間衝突回避)。次は S2 (装飾)
- [x] **Q33**: 25 **S2** (2026-07-09 完了) — 装飾を第一級状態に。`GraphDecoration` (抽象 record) + 5 種: `NodeBadgeDecoration` (エラー/警告/情報リング)・`EdgeHighlightDecoration`・`PortHintDecoration` (配線ヒント光り)・`PendingWireDecoration` (進行中ワイヤ=出発ポート→world 点)・`NodeInlineDecoration` (ノード内枠、Key で view が widget 解決)。**`AffectsLayout` 分類**: インライン枠のみ true (ノードサイズに効く=S3 幾何キャッシュのキー)、他はオーバーレイのみ false (進行中ワイヤ 60fps の根拠)。**`Map(NodeGraphDoc)`** は座標写像ではなく**存在フィルタ** (安定 id なので削除対象を落とすだけ)。`GraphDecorationSet` (SortKey ソート/Map/OfKind/AnyAffectsLayout) + `GraphDecorationTable` (owner 別・不変・Set/Remove/Map/All)。`GraphStateEffect`/`SetGraphDecorations`/`RemoveGraphDecorations` + `IGraphDecorationProvider` (同期 pull) + `GraphDecorationProviders.Collect`。`NodeGraphState.Decorations` 統合 (`Transaction.Build` が既存装飾を新 Doc へ写した後 effect を適用、undo/redo は `With` が retain)。**設計判断**: 装飾は呼び出し側が Doc と整合させてから ctor に渡す契約 (テキスト EditorState と同じ、ctor で再 Map しない=インスタンス churn 回避)。単体 `NodeGraphDecorationTests` 10 本 (AffectsLayout/Map 落とし/Set・Table/effect/編集追従/undo/provider)、全 1008 passed。**UI 非接続・Gallery 未参照 = golden 影響なし** (S1 の vk e2e 84/84 diff 0 が有効)。次は S3 (GraphGeometry)
- [x] **Q34**: 25 **S3** (2026-07-09 完了) — `GraphGeometry` (純射影)。**コアは Typography/TwoD 非依存を維持**: 自前の軽量 `Size`/`GraphRect`/`GraphWire`/`PortGeometry`/`WidgetSlot`/`GraphHit` (System.Numerics.Vector2 のみ) + サイズ測定は `NodeMeasure` デリゲートで view が注入 (`DiagramLayout.Arrange(spec, measure)` と同型)。`GraphConfig` (TitleBar/PortRow/PortRadius/WireTangent/EdgeHitTolerance…)。API: `NodeRect`/`PortAnchor` (入力=左端・出力=右端、タイトルバー下に行配置、折り畳みはタイトル中央)/`Wire` (3 次ベジェ、水平接線 = out→右/in→左)/`HitTest(world)` (ポート→本体→辺→空白の優先、上のノード優先、辺はベジェを 24 分割サンプルで距離判定・許容は screen px を zoom で world 化)/`WidgetSlots` (インライン枠をポート下に縦積み)/`WorldToScreen`/`ScreenToWorld` (screen=world*zoom+pan)/`ContentBounds` (fit-to-view)。**ステートフルなキャッシュ保持型** (`EditorGeometry` と同流儀): `SetState` で再構築し、レイアウト鍵 (id+Pos+サイズ+折り畳み+ポート署名+**インライン枠署名のみ**) が同じノードの `NodeLayout` を再利用 — オーバーレイ装飾/viewport 変化では再構築しない (配線中ワイヤ 60fps の根拠)。単体 `NodeGraphGeometryTests` 11 本 (矩形/アンカー/折り畳み/ワイヤ端点・接線/world↔screen 往復/HitTest 各種+最前面優先+辺/ContentBounds/キャッシュ Same[バッジ・viewport]・NotSame[移動・インライン枠])、全 1019 passed。**UI 非接続 = golden 影響なし**。次は S4 (canvas 接続 = NodeGraphView + ドラッグ/選択、golden 生成開始)
- [x] **Q35**: 25 **S4** (2026-07-09 完了) — canvas 接続。`GraphCommands` (純関数 `(state)→transaction`: AddNode/DeleteSelection/MoveNodes[複数=1 undo]/Select*/Connect、Luxel.NodeGraph)。`NodeGraphView` [UiComponent] (Luxel.Controls、Luxel.NodeGraph 参照): `NodeGraphState`/`GraphHistory`/`GraphGeometry` を保持し入力を Transaction 化。描画は色グループ別の子ノード (グリッド/ワイヤ/本体/ヘッダ/枠/選択枠/ポート/タイトル) を `ctx.Effect` でテーマ recolor + `ctx.Font.AppendText`。**pan/zoom は world コンテナノードの `Affine2D` 変換** (ヒットテストが自動追従、`_root` にヒット登録し `e.X/Y`→`ScreenToWorld`)。入力: ノード本体ドラッグ→移動 (プレビュー状態で描き、drop で `MoveNodes` 記録=1 undo)、空白ドラッグ→marquee 範囲選択、クリック→選択/クリア、ホイール (`onScrollPos`)→カーソル中心ズーム、キー (Ctrl+Z/Y・Ctrl+A・Delete・Esc)。公開 API: `NodeScreenCenter`/`ClientOf` (play 用クライアント座標)、`PanBy`/`FitToView`/`ResetView`/`Undo`/`Redo`/`IsSelected`/`NodePos`。**MD 差**: 幾何の `Size`→`NodeSize` (UI レイアウト Size と衝突回避)。story `Controls/NodeGraphView/Basic` (3 ノード Input→Process→Output + 2 辺、play=basic[選択/ドラッグ移動]・marquee[範囲選択]・keys[Ctrl+A/Delete/undo])。単体 `NodeGraphCommandTests` 6 本、全 1025 passed、**vk/dx golden 各 6 枚**、**full vk e2e 87/87 diff 0** (Reference/Overview 含め無回帰)。**制約 (ADR-0009 通り)**: PointerEvent にボタン/修飾キーが無いため、空白ドラッグが範囲選択に取られ**対話 pan ドラッグは延期** (pan は公開 API、zoom はホイールで対話可)。次は S5 (配線: ポートドラッグ→Connect/Disconnect)
- [x] **Q36**: 25 **S5** (2026-07-09 完了) — 配線操作。`GraphConnect` (core 純ヘルパ、`TryResolve`/`CanConnect`: 別ノード・向き逆・型キー一致・重複なしを判定し out→in に正規化。view とテストで共有)。`NodeGraphView` 拡張: ポートヒットで配線ドラッグ開始 (`Drag.Wire`)、`_pendingN` に進行中ワイヤ描画 (終端が互換ポートなら緑・そうでなければ赤、`node.Color` で可否色)、drop で `GraphConnect.TryResolve`→接続 (単入力ポートは既存辺を Disconnect して付け替え=1 undo、辺 id は max+1)。辺クリック→選択 (`_selWireN` に Primary で太く描画)、Delete で切断 (S1 の RemoveNode/Disconnect を DeleteSelection 経由)。公開 API: `EdgeCount`/`IsEdgeSelected`/`PortScreen`/`EdgeMidScreen` (play 座標)。**罠**: 進行中ワイヤは stroke なので `ContentColors` では色が乗らず (fill は乗る)、`_selWireN`/`_pendingN` は `node.Color` 方式に (DiagramBlock の stroke と同じ)。story `Controls/NodeGraphView/Wiring` (A→B→C、play connect[緑の進行中ワイヤ→接続]・disconnect[辺選択→Delete]・invalid[同方向は不可])。単体 `GraphConnect` 2 本 (正規化/自己・同方向・型不一致・重複を拒否)、全 1027 passed、**vk/dx golden 各 5 枚**、**full vk e2e 90/90 diff 0**。次は S6 (ノード内インライン widget + パレット + INodeCatalog)
- [x] **Q37**: 25 **S6** (2026-07-09 完了) — ノード内インライン widget + パレット。**`INodeCatalog`** (core、`NodeCatalogEntry(Kind/Label/Create(id,pos)→GraphNode)` + 素朴実装 `NodeCatalog`): ドメインが種別と工場を宣言 (core は具体ドメイン非依存)。**インライン widget ホスト** (`NodeGraphView`、TextEditorView 移植): `WidgetResolver` (Key→Widget) + `NodeInlineDecoration` の `WidgetSlots` を**画面空間レイヤ** (`_widgetLayer`) に `WorldToScreen` 配置で Realize (zoom でヒットがずれない選択)、`OnChildNeedsRealize` で再実体化、消えたキーは Dispose。`SetDecorations(owner,set)` 公開。**パレット**: 右クリック (`onContext`) → `ContextMenu.Open` (ADR-0007 の PopupPlacer 上) に `NodeCatalog.Entries` を並べ、選択でクリック位置に `AddNode` (id=max+1)。公開 API: `SlotScreenCenter` (play 用)。**罠修正**: `SetDecorations` が realize 前に呼ばれると `EnsureInit` 前で空グラフに装飾が乗り消える → `SetDecorations` 冒頭で `EnsureInit()` して source doc を先に読む。story `Controls/NodeGraphView/Widgets` (Gain ノードにスライダ + 右クリックパレットで Gain/Output 追加、play inline[スライダ操作]・palette[右クリック→追加])。単体 `NodeCatalog` 1 本、全 1028 passed、**vk/dx golden 各 4 枚**、**full vk e2e 92/92 diff 0**。次は S7 (自動整列 + fit-to-view + グリッドスナップ + ドメインデモ + Docs、**完了で 25 MD 削除**)
- [x] **Q38**: 25 **S7** (2026-07-09 完了) — 自動整列 + fit-to-view + グリッドスナップ + ドメインデモ + Docs。`GraphCommands.AutoLayout` (core、`DiagramLayout` の最長経路ランキングを**辺モデルで自前実装** = 依存ゼロ維持、サイズは `NodeMeasure` 外注、全ノード 1 tx=1 undo、循環は反復打ち切りで安全)。view: `AutoLayout()`/`FitToView()` (S4 既存)/`SnapToGrid` (ドロップ位置をノードごとにグリッド整合、`BuildMove`)。**ドメインデモ** story `Controls/NodeGraphView/AutoLayout` (式グラフ Const×2→Add→Output を `INodeCatalog` + AutoLayout で整列、汎用性を実証)。**Docs/NodeEditor** ページ新規 (DocsNodeEditor.cs、Order 42、コア/ビュー/機能/制約 + 4 ストーリーへのリンク)。単体 `AutoLayout` 2 本 (ランク左→右・同ランク縦積み・1 undo)、全 1030 passed、**vk/dx golden 各 2 枚**、**full vk e2e 93/93 diff 0** (デッドリンク無)。**→ ToDo 25 (S1〜S7) 完成・MD 削除。ADR-0009 全ステージ達成。M9 完了**

### M10 — エディタ Workbench (ADR-0010〜0014、[26](26-editor-workbench.md) の大プログラム。既存編集コンポーネントを束ねるシェル + 土台整備。**26 MD は全ワークストリーム完了まで残す**。設計のみ、未着手)

> 依存順の既定: WS-0 (0011) → WS-A (0012) / WS-B は並行 → WS-C (0010/0014) → WS-M (0013) → WS-D。各ワークストリームは独立に「次へ」で着手できる。詳細・罠・検証は [26](26-editor-workbench.md) に集約。

- [x] **Q39**: 26 **WS-0** — PointerEvent 拡張 (ADR-0011、2026-07-09 完了)。`PointerEvent` に `PointerButton Button` + `[Flags] KeyModifiers Modifiers` (+ Ctrl/Shift/Alt/Meta 便宜プロパティ) を追加、両 ctor は既定引数で旧呼び出し互換。`UiHost` の Click/PointerDown/PointerUp/PointerMove/ContextClick + `DragEv` に button/mods を貫通 (捕獲時ボタンを `_captureButton` に保持)。**Play** ドライバに修飾/ボタン注入 (`Click(x,y,mods,button)`/`Drag(...,button,mods)`/`RightClick`)。**実機経路**: `Win32Modifiers` (GetKeyState を LibraryImport、WndProc 同期呼び出し時に修飾を拾う) + `WM_MBUTTON` 追加 (NativeMethods.txt)、`WindowHost`/`AppWindow`/`IWindowContent`+`UiContent` が button/mods を転送 (**副次修正**: 実 Win32 の KeyDown が修飾を常に false で潰していたバグも解消)。**延期解消**: `TextEditorView` Alt+Click 追加キャレット (ポインタからのマルチカーソル)、`NodeGraphView` 中ボタンドラッグ pan + Ctrl+Click 追加選択。単体 `PointerEventTests` 3 (全 1037 passed)、story 追加 play `Controls/TextEditorView/MultiCursor/alt-click` + `Controls/NodeGraphView/Basic/modifiers`、**vk/dx golden 各 4 枚新規 + MultiCursor 既存 4 枚 caption 更新**、**full vk e2e 96/96 diff 0**。(注: 画像ストーリー 4 本が full-suite で async リソース読込レースにより稀に diff — 単独では diff 0、本改修と無関係の既存 flakiness)。次は WS-A (Q40)
- [ ] **Q40**: 26 **WS-A** — Markdown/リッチ文書を**テキストスタックの構成**に (ADR-0012、別プロジェクト無し)。テキストスタック拡張 (font-variant Mark + mixed-weight ラン + ブロック widget) → Markdown 装飾プロバイダ + widget リゾルバ (内容プロセッサを widget 化) + read-only 描画 → `Kit.Docs()` 差し替え + Docs/ADR 全 11 モジュール移行 → ブロック単位コマンド + 編集モード → RichTextEditor/TextArea/Luxel.Document 編集核 削除。**golden 全再生成を伴う最大ワークストリーム** (着手中: 2026-07-09 — **S(A1) 完了、次は S(A2)**)。**S(A1) 済**: font-variant Mark (`MarkDecoration` に `FontVariant? Variant` + `float? FontScale`、`AffectsLayout` 拡張) + `EditorConfig` に Bold/Italic/BoldItalic フォントスロット + `FontFor` + `EditorGeometry.BuildLine` の行内 mixed-run (色+変種+倍率でラン分割、`SpanStyle.Font/Size`、`LineKey` に変種署名) + `TextEditorView` の変種フォント props。TextLayout/描画は無改修 (ラン混在対応済み)。単体 `EditorFontVariantTests` 4 (全 1041)、story `Controls/TextEditorView/RichText` (見出し/太字/小サイズ、vk/dx golden)、**full vk e2e 97/97 diff 0**。**S(A2 部分) 済** (2026-07-09): `MarkdownDecorations.Build` (純パーサ: 見出し #..###### = Bold+サイズ倍率、太字 `**`、斜体 `*`、インラインコード `` ` `` = Mono+背景、記法マーカは淡色) + `MarkdownProvider` (`IDecorationProvider`、キャッシュ) + `FontVariant.Mono` 追加 (EditorConfig.MonoFont/FontFor)。単体 `MarkdownDecorationsTests` 9 (全 1050)、story `Controls/TextEditorView/Markdown` (vk/dx golden)、full vk e2e 98/98 diff 0。**block-level 構文も追加** (2026-07-09): 引用 `>` (BlockDecoration 左バー+インデント)・箇条書き/番号リスト (マーカ淡色)・コードフェンス ` ``` ` (LineDecoration 行背景 + Mono)。**S(A2b) 済** (2026-07-09): ブロック **widget** = `BlockWidgetDecoration` (複数ソース行 [From,To) を占有)。geometry は**先頭行を宣言高さの全幅スロット・残りを高さ0に畳む** (行↔ソースの 1:1 不変 → キャレット/ヒット無傷)。`BuildBlockLine` + Rebuild 分岐 + LineKey 署名 + WidgetSlots 全幅スロット。view は既存 WidgetResolver/HostWidgets がそのまま実体化。単体 `EditorBlockWidgetTests` 3 (全 1053)、story `Controls/TextEditorView/BlockWidget` (中間 2 行→全幅パネル、vk/dx golden)、full vk e2e 99/99 diff 0。表/mermaid/数式/埋め込みの土台完成。**リンク + read-only も済** (2026-07-09): `[text](url)` = アクセント色+下線・括弧/URL 淡色 (MarkdownProvider)、`TextEditorView.ReadOnly` (`Apply` で `DocChanged` を無視、選択/スクロールは可) — 単体 +1 (全 1054)、Markdown story を link+ReadOnly 入りに (type しても不変を play で実証)、e2e 99/99。**文書レンダラ部品 packaging も済** (2026-07-09): `MarkdownDoc.Create` (TextEditorView + MarkdownProvider を read-only + 折返しで束ねる 1 ファクトリ、`Kit.Docs()` 差し替えの部品) + `MarkdownDecorations.Headings` (見出し抽出 = TOC/`story:` ナビの素、純関数)。単体 +2 (全 1056)、story `Controls/TextEditorView/MarkdownDoc` (折返す段落 + 全 Markdown を 1 呼び出しで、vk/dx golden)、full vk e2e 100/100 diff 0。**リンクナビの土台も済** (2026-07-09): `MarkdownDecorations.Links` (リンクの範囲+URL を抽出、純関数) + `TextEditorView.OnClickOffset` (クリック→ソースオフセット通知)。caller は offset→link→navigate で配線可 (`story:` ナビ)。単体 +1 (全 1057)、Markdown story の play に OnClickOffset 発火 assert 追加、full vk e2e 100/100 diff 0。**ブロック widget 自動高さも済** (2026-07-09): `BlockWidgetDecoration.Height<=0` = 自動。geometry に `BlockHeight` デリゲート (view が実測を供給、`BlockHeightOf` が宣言>0 優先・自動は実測・未測定は 24 見積り、LineKey も実測込み)。view は auto block を高さ無制約で realize→自然高さ測定→変化時 `_blockDirty`→次フレーム `Refresh` で採用 (`AddAnimation` フックで**再入回避・収束**)。単体 +1 (全 1058)、story `Controls/TextEditorView/BlockWidgetAuto` (3 行パネルが実測高さで確保・Outro が直後、vk/dx golden)、full vk e2e 101/101 diff 0。**埋め込みライブ UI の前提が完成**。**埋め込みライブ UI 機構も済** (2026-07-09): ` ```embed <key> ` フェンス → MarkdownProvider が自動高さ block widget を出し、view の `WidgetResolver` が key から実 Widget を解決 (フェンス行は畳んで隠す)。`Kit.Docs()` の hole 埋め込み相当を新スタックで実現。単体 +1 (全 1059)、story `Controls/TextEditorView/Embed` (Button 入りパネルを doc に埋め込み、vk/dx golden)、full vk e2e 102/102 diff 0。**→ 新レンダラは markdown + 埋め込みで Kit.Docs のコア機能パリティ達成** (mermaid/数式は「特定の embed widget」= Diagram/MathText を key で解決するだけ)。**本文付き埋め込みも済** (2026-07-09): 埋め込みフェンスの**本文 (body)** を捕捉し `EmbedRef(Key, Body)` として block widget の Key に。resolver が種別 + 本文で解決 (` ```embed mermaid\n<図>\n``` ` → 図ソースを Body で渡す = **mermaid/数式アダプタの土台**)。単体 +1 (全 1060)、Embed story に本文付き "note" 埋め込み追加 (vk/dx golden)、full vk e2e 102/102 diff 0。**mermaid/数式アダプタも済** (2026-07-09): ` ```embed mermaid|math ` の本文を既存 `Luxel.Diagram.Factories.DiagramBlock` / `Luxel.MathText.Factories.MathBlockView` で図/式 widget に解決 (内容プロセッサ再利用、自動高さで載る)。story `Controls/TextEditorView/DocEmbeds` (実 mermaid フローチャート + 分数/√ 数式が doc に描画、vk/dx golden)、full vk e2e 103/103 diff 0。**→ 新スタックは `Kit.Docs()` の全機能 (markdown + 装飾 + ブロック widget + ライブ UI 埋め込み + mermaid + 数式 + リンク + read-only + 折返し) をカバー = 機能パリティ完全達成**。**read-only レンダリングの質も向上** (2026-07-09): ① **マーカ非表示** — `MarkDecoration.Hidden` (幅0 で畳む、`Seg(Src,0,IsWidget,Key=null)` で既存 widget 折り畳み写像を再利用しキャレット/選択は無傷) + `MarkdownProvider(hideMarkers)` + `MarkdownDoc.Create` は ON。read-only 文書は `#`/`**`/`` ` ``/`>`/`-`/`[]()` が完全非表示に (編集/live-preview 用に false は従来の淡色)。単体 +2 (hidden geometry / hideMarkers、全 1062)、MarkdownDoc/Embed/DocEmbeds golden 再生成 (vk/dx)。③ **コードのシンタックスハイライトを装飾で 済** (2026-07-09): `MarkdownDecorations.Build(highlighter)` がフェンス言語 (```` ```csharp ````) で本文を行ごとに `ISyntaxHighlighter.Tokenize` → トークンごとに `Mark.Foreground`(=`CodeDecorations.TokenColor`)。**実テキスト + 色 Mark = 選択可能・widget 化しない**。`MarkdownProvider`/`MarkdownDoc.Create` に highlighter 注入 (caller が TextMateHighlighter を渡す = Controls は非結合)。単体 +1 (全 1063)、MarkdownDoc story を ```csharp フェンス + highlighter に (var/数値/コメントが色分け、vk/dx golden)。④ **行全体マーカの高さ畳み + 折返し行間 済** (2026-07-09): (a) 行全体が非表示 (フェンス区切り等マーカのみの行) は `EditorGeometry` が**高さ0 に畳む** (空行が残らない)。(b) 折返し**段落内**の行送りを `EditorConfig.WrapLineHeight` でブロック間 (1.5) と分離 → `MarkdownDoc` は段落内 1.3 で詰める (タイポ的に段落内 < ブロック間が正)。単体 +2 (WrapLineHeight で段落総高が縮む・単一行はブロック行送り維持 / 全非表示行=高さ0)、MarkdownDoc golden 再生成 (段落が詰まり・コードブロック前後の空行が消えた)。**残 (任意)**: 編集モードのカーソル行マーカ表示 (live-preview)。**DocString→新スタック橋も済** (2026-07-09): `MarkdownDoc.FromDoc(DocString)` — 既存の `Docs($"...{Widget}...")` 記法をそのまま新スタックで描く。`MarkdownDecorations.Build` の埋め込み判定を `embedKinds` で一般化 (`` ```embed `` に加え `` ```luxel-ui ``/`` ```mermaid `` 等を埋め込み扱い)、`DocString.Md`/`HoleWidgets` 公開、FromDoc の resolver が `luxel-ui`→hole widget・外部ドメイン (mermaid/数式) は `fences` で注入。単体 +2 (embedKinds 認識/既定は無回帰)、story `Controls/TextEditorView/DocBridge` (DocString 記法のページが live Button + mermaid 込みで新スタック描画、vk/dx golden)、全 1067。**→ 既存 Docs ページは `Docs(...)` → `FromDoc(...)` の実質 1 行変更で移行できる**。**DocsIndex 新スタック対応も済** (2026-07-09): `TextEditorView.DocSource` (build 時に取れる markdown ソース、`MarkdownDoc.Create` が設定) + `DocsIndex.FindMarkdownDoc`/`LinkBroken` で、新スタック docs も全文検索 (`MarkdownDecorations.Headings`) + デッドリンク検証 (`Links`→story:/#アンカーを `RichTextEditor.Slug` で確認) の対象に。実機起動で **docs index 64 pages・dead links 0** (新スタック docs の story: リンクも検証済み)。**→ 移行の前提が全部揃った** (実ページを FromDoc に切替えても検索・リンク検証が効く)。**日本語フォールバックフォントも対応** (2026-07-09): `MarkdownDoc.Create`/`FromDoc` に `FontCollection? fonts` (`ed.Fonts` = JP/絵文字フォールバック) → 日本語 Docs/ADR がトーフにならず描画 (golden で 日本語段落を実証)。**stretch/fill サイズ対応も済** (2026-07-09): `TextEditorView.Fill` (true = 固定 W/H でなく `PerformLayout` の制約 `c.MaxW/MaxH` を採用、変化時は `_layoutDirty`→次フレームで `BuildConfig` 再構築 = 折返し幅も追従、再入回避は既存 `AddAnimation` フックに相乗り。`MaxIntrinsicWidth` は Fill 時 100000 を返し親に「幅いっぱい」を要求)。`MarkdownDoc.Create`/`FromDoc` に `bool fill` (width/height は fallback 見積り扱い)。レイアウトパス依存のため単体でなく golden で担保 — story `Controls/TextEditorView/MarkdownFill` (440×240 枠に入れ fallback 160 でなく枠幅で折返すのを実証、vk/dx golden)、full vk e2e 105/105 diff 0、全 1067。**→ 実 Docs ページをペインいっぱいに描く前提が完成。S(A3) 実移行 パイロット 1 ページ 済** (2026-07-09): 新スタック docs ヘルパ `DocsKit.DocNew(ctx, content, toc)` (= `Docs(ctx,...)` のドロップイン): `MarkdownDoc.FromDoc` に日本語フォント + TextMate ハイライト + mermaid/math フェンス (dict) + `fill:true` + `toc` を配線し、`OnClickOffset`→`Links` でクリック位置のリンクを解決 (`NavigateDoc`: `story:`→Navigate・`#アンカー`→見出しスクロール・http→ブラウザ・他→Log)。土台追加: `MarkdownDoc.InsertToc` (H1 直後にアンカー付き TOC リストを挿入、`RichTextEditor.Slug` で本文と一致、hole 番号不変) + `TextEditorView.ScrollToSource(offset)` (`#アンカー`/TOC ナビ用に指定オフセット行を上端へ、`_geo.CaretRect`)。**パイロット = `ADR/0001`** (純 markdown + toc + link) を `WithDocFonts(Docs(...))` → `DocNew(...)` に置換し `ctx.Play(Snap)` で golden 付与 (vk/dx)。実機で **docs index 65 pages・dead links 0** (ADR0001 が新スタック経路で索引され story:/# 全解決)、full vk e2e 106/106 diff 0、単体 `InsertToc` 2 本 (全 1069)。**ホール付きページも移行 済** (2026-07-09): `Docs/GettingStarted` (ライブ `{{counter}}` widget hole + toc + story:/外部リンク + csharp フェンス) を `WithDocFonts(Docs(...))` → `DocNew(...)` に置換。golden は ①先頭 (見出し/TOC) に加え、play で「最初の UI」見出しへ `ScrollToSource` して ②`counter` snap を追加 = **埋め込みカウンタ ([-] 0 [+]) が全幅キャンバスに実描画 + 直下の csharp がシンタックス色分け (選択可) + story: リンク**を実証。既存 `.vk/.dx` golden は新スタック描画に差替、`.counter.vk/.dx` を新規。docs index 65 pages・dead links 0 (GettingStarted の story: 全解決)、full vk e2e 106/106 diff 0 (snap 146)、全 1069。**→ FromDoc ブリッジが実ページで block hole + ハイライト + リンク + fill + フォントを全部描けることを確認**。**ADR 全 14 本 移行 済** (2026-07-09): ADR 0002〜0014 (全て純 markdown + `toc: true` + `DocsFences`) を `WithDocFonts(Docs(ctx,...))` → `DocNew(ctx,...)` に一括置換 (sed、機構は検証済み ADR0001 と同一・差はテキストのみ)。ページに play/snap は付けず、検証は **docs index 65 pages・dead links 0・skip 0** (全 ADR が新スタック経路で索引され story:/# 全解決) + full vk e2e 106/106 diff 0。**Docs テキスト章 + API リファレンス章 移行 済** (2026-07-09): (a) **DocsText** (Typography/Editor) を `DocNew` へ — Typography に snap 追加し **mermaid フローチャート (DiagramBlock) が実描画**されるのを golden で実証 (`StorySource` は生 markdown 注入なので hole でなくそのまま、`StoryRef` は widget hole)。(b) **DocsApi** 全 8 ページ (Reference/Overview + Ui/2D/Core/Text/Animation/ThreeD/Runtime、共通 `NamespacesPage` の 2 呼出だけ) を `DocNew` へ — **`ApiTable`/`TypeApiTable` の表 widget hole** が FromDoc 経路で実描画されるのを Reference/Overview の `table` snap (最初の ## へ ScrollToSource) で実証。**アンカー slug バグも修正**: `MarkdownDoc.Slug` を新設 — 見出しに `(...)` があると `RichTextEditor.Slug` は括弧を残し、TOC の `](#…(…))` の内側 `)` がリンク URL を途中で閉じて `#アンカー` が壊れる (DocsText で dead links 5)。新 Slug は括弧 `()（）[]` を除去し、InsertToc / NavigateDoc / DocsIndex.LinkBroken で共通使用 (旧 `RichTextEditor.Slug` は old stack 用に不変、手書きアンカーは `#書けるもの` 1 件のみで括弧無し=無影響)。単体 +1 Slug、+3 bullet と合わせ全 1073、影響 golden: Typography (mermaid+括弧見出し TOC の stray `)` 消失で差替) vk/dx・Reference/Overview vk/dx 差替 + `.table` 新規、docs index 65 pages dead links 0、full vk e2e 107/107 diff 0。**ライブ埋め込み無しの章も移行 済** (2026-07-09): `Docs/NodeEditor` + `Docs/Architecture` (共に mermaid のみ・ライブ埋め込み無し、Architecture は DocsHome の該当ストーリーだけ) を `DocNew` へ。両方 snap 追加で mermaid 図の実描画を実証 (vk/dx golden)、full vk e2e 109/109 diff 0、docs index 65 pages dead 0、全 1073 (GettingStarted vk golden は環境 AA ドリフト 18B で再生成=良性、コードパス不変・再実行で安定)。**⚠️ ブロッカー発見 — ライブ/GPU デモ埋め込みページは新スタックで device lost**: `Docs/Scripting` (LiveCsx) / `Docs/Strudel` (REPL) を `DocNew` へ移すと Vulkan `ErrorDeviceLost` で落ちる。原因は `TextEditorView.HostWidgets()` が**毎 `Refresh()` で各埋め込み widget を `Scope.Release()`+`Realize()` し直す**こと — 静的埋め込み (表/mermaid/RichText) は自動高さが収束して無害だが、**毎フレーム GPU/audio を持つライブデモ**は scope を毎フレーム破棄→再構築され use-after-free → device lost。旧スタック (Kit.Docs/RichTextEditor) は 1 回実体化して保持するので無事だった。測定上限を有限化 (∞→8192) しても直らず=高さでなく**再実体化サイクル**が主因。**→ 修正 済** (2026-07-09): `HostWidgets()` が既に実体化済みで**箱 (位置+サイズ) が不変な埋め込みを再実体化しない**ように (`isNew || h.Rect != slot.Rect` のときだけ `RealizeWidget`)。旧スタックと同じ「1 回だけ実体化して保持、あとは埋め込み自身の scope (Effect/AddAnimation) が自己更新」。自動高さは箱変化 (新規・高さ確定・折返し幅変化) で組み直すので収束は維持。スクロールは `_content` の transform で動き・実クリックは `ComputeWorldNow` でヒットするので再実体化不要 (再実体化が古くするのは programmatic `d.Click(widget)` の `WorldPos` だけ=実害小)。`Docs/Strudel` (ライブ REPL) / `Docs/Scripting` (LiveCsx CodeEditor) を `DocNew` へ移行して device lost せず描画されるのを golden で実証 (=回帰テスト)、full vk e2e 109/109 diff 0。**GPU シーン埋め込みの第 2 問題も修正** (2026-07-09): 上記の「1 回だけ実体化」でも自動高さ確定の 2 回目実体化が残り、GPU シーン (`GpuView`) は再実体化 = `SceneGuard` の Dispose→Init 往復で描画中リソースが壊れ device lost していた (Triangle で確認)。**幅が同じなら再実体化せず位置 (ルートノード transform) と clip だけ更新する** `RepositionHosted` を追加 — widget は初回に高さ無制約 (∞) で自然高さのまま実体化済みなので確定時は clip を広げれば全体が見え、位置が動いても transform 差し替えで追従 (実クリックは `ComputeWorldNow` が transform を辿るのでヒット追従)。折返し幅変化のときだけ組み直す。`Docs/FirstTriangle` (Triangle 1 本) + `Docs/TwoD` (**GPU デモ 7 本積み**) を snap 付き移行して device lost せず描画を実証。**→ CodeEditor/audio/UI/GPU シーン 全埋め込み型が新スタックで安全**。**残チャプタも全移行 済** (2026-07-09): DocsGpu (7)・DocsMotion (2)・DocsUi (4)・DocsRuntime (6)・DocsHome/FirstTriangle を `DocNew` へ (sed 一括)。full vk e2e 111/111 diff 0、全 1073、docs index 65 dead 0。**→ Docs/ADR 全ページ移行完了 (残は DocsMeta のみ = インライン hole 1 件で FromDoc 未対応 → S(A3)③)**。**S(A3)③ インライン hole 対応 + DocsMeta 移行 済 = S(A3) 完了** (2026-07-09): DocString の `[￼](luxel-ui:N)` インライン hole を**自動サイズ行内 widget** で解決。(a) `WidgetDecoration` に自動サイズ (W/H<=0) — `EditorGeometry.WidgetSize` デリゲート + `WidgetSizeOf` (宣言>0 優先・自動は実測・未測定は見積り)、`BuildLine`/`LineKey` が採用サイズを使う。(b) view が自動サイズ行内 widget を**幅高さ無制約で実測** (`_autoWidgetKeys`/`_widgetMeasured`/`MeasureAutoWidget`、block 自動高さと同じ _blockDirty フィードバック)。(c) `MarkdownDecorations.Build` が `[text](kind:body)` で kind が embedKinds なら全体を `WidgetDecoration(EmbedRef(kind, body))` に置換 (テキストリンクにしない)、FromDoc の resolver は EmbedRef を block と同じ経路で holes に解決。**DocsMeta** (Authoring: 行内 Badge/Button の `:inline` hole + block counter + Orbit 2D GPU 埋め込み、Gallery、Contributing) を `DocNew` へ。Authoring に `inline` snap 追加 = **緑 Badge「Ready」+ 青 Button「押す」が行内に描画**されるのを実証。単体 +2 (inline hole → WidgetDecoration / 未知スキームはリンク維持、全 1075)、full vk e2e 111/111 diff 0、docs index 65 dead 0。**残 (任意)**: 行内 widget のベースライン揃え (現状やや上寄り)。**→ S(A3) 実移行 完了 — Docs/ADR 全ページが新スタックで描画 (旧 RichTextEditor 依存は DocsIndex の FindDocEditor フォールバックのみ)**。**S(A4) Live Preview 編集モード 済** (2026-07-09): `MarkdownDecorations.Build` に `reveal(pos)` 述語 — hideMarkers 下でも reveal 行 (キャレット行) はマーカを畳まず淡色 raw で見せる (Typora 風)。`Marker`/list bullet が `Hide(pos)=hideMarkers && reveal!=true` を見る。`MarkdownProvider(livePreview)` が `state.Selection.Ranges` の行を reveal 集合にし、キャレット移動で再 Build (キャッシュ鍵に reveal 署名)。`MarkdownDoc.Create(editable)` = ReadOnly 解除 + livePreview。story `Controls/TextEditorView/LivePreview` (初期=見出し行 `#` raw・他行整形 → クリックで別行へ reveal 追従、vk/dx golden 2 枚)。単体 +2 (reveal 行のみ raw / reveal リスト行は bullet 出さず raw、全 1077)、full vk e2e 112/112 diff 0。**S(A5) 旧削除 — 依存棚卸し + フェーズ計画** (2026-07-09): 旧スタック消費者は多数で相互依存 → 段階移行。**旧エディタ実消費者** (要移行): ScriptingStory (CsxBlock=CodeEditor→移行済 / NotebookCell=CodeEditor + `Luxel.Document.FencePayload`)、ScriptHotReloadStory (CodeEditor)、LiveCodeStory (TextArea + RichTextEditor)、ReplConsole (TextArea)。**旧デモ story** (削除/置換 + docs リンク修正要): CodeEditorStory (Controls/CodeEditor/Basic・Search・Completion)、TextControlStories (RichTextEditor/Basic・MarkdownEditor/Hybrid・VisualSource・Embeds・TextArea/Basic・Scroll・RichText/Basic)。**旧クラス** (最後に削除): CodeEditor.cs・RichTextEditor.cs・RichTextView.cs・TextArea.cs・Docs.cs・Luxel.Document 編集核。**その他**: DocsIndex.FindDocEditor フォールバック、DocsKit.WithDocFonts/DocsFences、旧スタックの単体 (CodeEditorTests・DocumentEditorRichTests・DocsTests・InlineWidgetTests・SearchTests)。**フェーズ**: ①実消費者を TextEditorView へ (Scripting/ScriptHotReload/LiveCode/Repl) → ②MarkdownEditor 系を新スタック editable(Live Preview) へ + 旧デモ story 削除 + docs リンク付替 → ③旧クラス + 単体 + フォールバック削除 → ④Luxel.Document 編集核削除。**Phase① 一部 済**: **ScriptingStory の CsxBlock (LiveCsx playground)** を `CodeEditor` → `TextEditorView` へ (API 互換: `LanguageService`/`CompletionOpen` 一致、色分け/診断/現在行は `SyntaxHighlightProvider`/`DiagnosticsProvider`/`CurrentLineProvider` で)。LiveCsx の Ctrl+Space 補完 (Roslyn 候補) + 診断 + Run→Widget が動くのを golden で実証、Docs/Scripting 埋め込みも再生成、full vk e2e 112/112 diff 0、全 1077。**ScriptHotReload + ReplConsole も移行 済** (2026-07-09): `Demos/Scripting/HotReload` (CodeEditor→TextEditorView、色分け/現在行 provider、補完なし) + `ReplConsole` (`Demos/Scripting/Repl` の入力、TextArea→TextEditorView、plain 入力+mono)。継続セッション (a=21→a*2=42) が動くのを golden で実証、full vk e2e 112/112 diff 0、全 1077。**残 Phase①**: NotebookCell (ScriptingStory 内、`Luxel.Document.FencePayload` 依存 → notebook の markdown/fence パースごと新スタック化が要る)・LiveCodeStory (TextArea 入力 + RichTextEditor 出力)。**→ 残ったら Phase② (MarkdownEditor 系 + 旧デモ story 削除)**。**残 (S(A3)) ブロック中**: DocsGpu (GPU デモ 19)・DocsRuntime (Scripting/Audio)・DocsStrudel・DocsMotion・DocsUi・DocsHome/FirstTriangle。**DocsMeta** は別途インライン hole 1 件で FromDoc 未対応。**次スライス候補**: (優先) 埋め込みホスティングの再実体化を修正 → 残チャプタ解禁。または先に S(A3)③ インライン hole 対応。③ インライン hole (実使用 1 箇所) → S(A4) 編集モード → S(A5) 旧削除。**リスト `•` bullet 表示も済** (2026-07-09): read-only (`hideMarkers`) のリストは源の `- `/`N. ` を畳み、`LinePrefixDecoration` (番号/箇条書き用に既存) で行頭に `• `/`1. ` を淡色表示。ネストは `新string(' ', indent) + glyph` でインデント保持、番号リストは番号をそのまま。編集モード (`hideMarkers=false`) は従来どおりマーカ淡色・prefix なし。`MarkdownDecorations.ListBullet` ヘルパ。単体 +3 (bullet/ネスト+番号/編集モード非 bullet、全 1072)、影響 golden 5 枚差替 (ADR0001・GettingStarted×2・MarkdownDoc・MarkdownFill、vk/dx)、full vk e2e 106/106 diff 0。→ read-only 文書のリストがベタ行でなく箇条書きに見えるように。**(旧 memo)**: `Kit.Docs()` を `MarkdownDoc` + embed resolver へ差し替え + Docs/ADR 全 11 モジュールの holes→```embed 記法書き換え (golden 全再生成、影響最大 — **1 ページ移行で golden 運用を固めてから展開**。DocsIndex 全文検索の RichTextEditor 依存も差し替え要) → S(A4) 編集モード → S(A5) RichTextEditor/TextArea/Luxel.Document 削除
- [ ] **Q41**: 26 **WS-B** — CodeEditor 削除。5 ストーリーを TextEditorView + プロバイダへ移植 → `CodeEditor.cs` 削除。WS-A と並行可
- [ ] **Q42**: 26 **WS-C** — Workbench コア + 基盤 UI (ADR-0010/0014)。`Luxel.Workbench` (IEditorDocument/Workspace/DockTree/IDocumentStore) + `DocumentTabs`/`DockHost`/`StatusBar`/`PropertyGrid`/`AssetBrowser`。純ロジックは単体、コントロールは golden
- [ ] **Q43**: 26 **WS-M** — メニュー/コマンド (ADR-0013)。`CommandRegistry` を単一の真実に MenuBar/CommandPalette/Toolbar/Keymap を生成。寄与登録 + アクティブ doc の文脈メニュー合成
- [ ] **Q44**: 26 **WS-D** — 具体エディタ作成。D1 (既存 4 view を host) → D2 (open/save 配線) → D3 (新ドメイン 1 本 = Slang or マテリアルグラフ) → D4 (PropertyGrid 実証)。**Gallery を Workbench で再構築 (既定レイアウト再現で golden 中立)**。完了で **26 MD 削除**。D5 (ビューポート系シーンエディタ) は別 ADR・スコープ外

## 順序の根拠 (要約)

- **13 が最初**: 全 golden 再生成タスク — golden が増える前ほど差分が小さい。
- **19-A (publish 早回し) を機能実装より前に**: アセットパス/shaders/フォント同梱の穴は早く見つけるほど各機能タスクが正しい前提で書ける。
- **21-① を M2 の前に**: ECS スケール対応・DevStats・fps 化は、ゲーム機能を作っている最中のデバッグ効率に直接効く。gizmo だけは対象機能の後 (②③)。
- **M2 内の順**: 15/17 は独立で軽い → 18 (最大・16 のテクスチャパーティクルの前提でもある) → 16 → 10 → 01。
- **M4 は 03→04→05→09**: 各 MD の推奨順そのまま (小さく積み上げ)。
- **M6 は最後**: ゲーム完成 (このリポジトリの当面のゴール) に寄与しないため。ユーザーの指示があれば前倒しして良い。

## 運用メモ

- キューの分割ステージ (Q04/Q05/Q12〜14 の 19・21、Q23〜27 の 22、**Q39〜44 の 26**) は **MD を消すタイミングに注意** — 全ステージ完了まで残す。
- git worktree で作業する場合は tools/ junction を忘れない (README/メモリ参照)。
- 検証 GPU が無い環境では e2e は Skip される — その場合は「単体テスト + ビルド」までで完了とし、キューに `(e2e 未実施)` を残して次のセッションで実機確認する。
